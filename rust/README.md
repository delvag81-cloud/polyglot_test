# Rust calculator

## Установка
Windows/macOS: `rustup` и `cargo` (проверить `rustc -V`, `cargo -V`).

## Запуск тестов
```bash
cargo test
```

## Что это демонстрирует
- Модульные тесты встроены в язык.
- `panic!` при делении на ноль.
