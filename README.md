# Polyglot Calculators & QA/AQA Training

Полиглотный учебный репозиторий с калькуляторами и автотестами на **7 языках**:  
Python, Node.js, PHP, Java, Ruby, Rust, Go.

Цель — расширить кругозор QA/AQA-инженеров, дать понимание разных экосистем и показать, как тесты и CI работают не только в Python, но и в других языках.

---

## 📦 Содержимое проекта

```
polyglot-calculators/
│
├── python/
├── node/
├── php/
├── java/
├── ruby/
├── rust/
├── go/
│
├── install_all_mac.sh
├── install_all_win.ps1
│
└── .github/workflows/polyglot-ci.yml
```

Каждая папка языка содержит:

- Мини-калькулятор с 4 операциями (`+ - * /`);
- Исключение при делении на ноль;
- Автотесты;
- README по запуску.

---

## ⚙️ Установка окружения

Для удобства есть два универсальных скрипта:

### **macOS**

```bash
./install_all_mac.sh --base-url=https://your-local-server/dist
```

### **Windows**

```powershell
powershell -ExecutionPolicy Bypass -File install_all_win.ps1 -BaseUrl https://your-local-server/dist
```

Скрипты ставят:

- Python
- Node.js
- PHP
- Temurin JDK
- Ruby
- Rust (через rustup-init)
- Go

И **спрашивают подтверждение** перед установкой каждого компонента.

---

## 🧪 Запуск тестов вручную

### Python

```bash
cd python
python -m unittest -v
```

### Node.js

```bash
cd node
node calc.test.js
```

### PHP

```bash
cd php
php CalcTest.php
```

### Java

```bash
cd java
javac -d out src/main/java/Calculator.java src/test/java/CalculatorTest.java
java -cp out CalculatorTest
```

### Ruby

```bash
cd ruby
ruby calc_spec.rb
```

### Rust

```bash
cd rust
cargo test --quiet
```

### Go

```bash
cd go
go test -v
```

---

## 🚀 GitHub Actions

В репозитории есть автоматический workflow:

```
.github/workflows/polyglot-ci.yml
```

Он:

- ставит нужные версии окружений,
- запускает тесты на всех 7 языках,
- гарантирует, что код рабочего состояния,
- показывает студентам, как выглядит профессиональный CI/CD.

Запуск происходит автоматически при:

- `push`
- `pull_request`

---

## 🎯 Зачем это QA/AQA

Этот проект показывает, что тестирование — не “кликать кнопки”, а **понимать код, стек и инфраструктуру**.  
QA, который разбирается в нескольких языках, быстрее читает логи, увереннее пишет автотесты, проще общается с разработчиками и точно знает, где искать источники ошибок.

---

Репозиторий сделан для моего курса по AQA
